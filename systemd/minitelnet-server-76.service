[Unit]
Description=MinitelNet WebSocket server
After=network.target

[Service]
User=pi
WorkingDirectory=/home/pi/minitelnet/server
ExecStart=/home/pi/minitelnet/server/.venv/bin/uvicorn server:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
